({
	handleSendToDisti : function(component, event, helper) {
		helper.handleSendToDisti(component, event, helper);
	},
    doInit : function(component, event, helper) {
		helper.doInit(component, event, helper);
	},
    deleteSecondaryContact : function(component, event, helper) {
		helper.deleteSecondaryContact(component, event, helper);
	},
    handleCancel : function(component, event, helper) {
		helper.handleCancel(component, event, helper);
	},
    handleErrors: function(component,error,helper){
        helper.handleErrors(component,error,helper);
    }
})